import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Wordle</h1>
            <p>Guess the word and win!</p>
        </header>
    );
};

export default Header;